Query:
  type: object
  config:
    description: "FarmersMarket ORM repository"
    fields:
      farm:
        type: "Farm"
        args:
          id:
            type: "Uuid"
        resolve: "@=resolver('Farm', [args])"
      farms:
        type: "[Farm]"
        resolve: "@=resolver('Farm', [args])"